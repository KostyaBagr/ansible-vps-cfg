all:
  hosts:
    server-1:
      ansible_host: "{{ lookup('env', 'VPS_IP') }}"
      ansible_connection: ssh
      ansible_user: "{{ lookup('env', 'VPS_USER') }}"
      ansible_password: "{{ lookup('env', 'VPS_USER_PASSWORD') }}"
